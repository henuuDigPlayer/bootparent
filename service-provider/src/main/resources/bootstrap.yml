spring:
  application:
    name: provider-service
  profile: dev
  cloud:
    config:
      profile: ${spring.profile}
      discovery:
        enabled: true
        serviceId: config-server
      name: ${spring.application.name}
      label: master
eureka:
  instance:
    hostname: 183.131.202.13
    service-url:
      defaultZone: http://183.131.202.13:8000/eureka/
ribbon:
  # ribbon刷新时间
  ServerListRefreshInterval: 30000
feign:
  hystrix:
    enabled: true